{% extends 'base.html.twig' %}

{% block title %}Pointage{% endblock %}

{% block body %}
    <h1>Pointage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Nom Utilisateur</th>
                <td>{{ utilisateur.Nom }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ pointage.Date ? pointage.Date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Duree</th>
                <td>{{ pointage.Duree ? pointage.Duree|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('pointage_index') }}">back to list</a>

    <a href="{{ path('pointage_edit', {'id': pointage.id}) }}">edit</a>

    {{ include('pointage/_delete_form.html.twig') }}
{% endblock %}
